{
  "name": "agri-zkp-circuits",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.js",
    "compile": "circom circuits/compliance.circom --r1cs --wasm --sym -o build -l node_modules",
    "build": "npm run compile && node scripts/setup.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  }
}
